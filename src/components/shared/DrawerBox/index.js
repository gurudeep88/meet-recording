import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
});

export default function DrawerBox({list, state, toggleDrawer}) {
  const classes = useStyles();

  return (
    <>
    </>
  );
}
